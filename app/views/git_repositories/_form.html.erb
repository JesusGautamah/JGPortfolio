<%= form_with(model: git_repository, local: true) do |form| %>
  <% if git_repository.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(git_repository.errors.count, "error") %> prohibited this git_repository from being saved:</h2>

      <ul>
      <% git_repository.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
